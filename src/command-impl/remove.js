'use strict'

const path = require('path')
const { green, yellow, blue, red} = require('colors')

const remove = async (inputParams) => {
  const {
    credentials, state, args,
  } = inputParams
}


module.exports = {
  removeImpl : remove
}
